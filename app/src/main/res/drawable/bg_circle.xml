<!-- res/drawable/bg_circle.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="#FFFFFF"/> <!-- Màu nền (có thể thay đổi) -->
    <size android:width="20dp" android:height="20dp"/> <!-- Kích thước hình tròn -->
    <stroke android:width="1dp" android:color="#CCCCCC"/> <!-- Viền (có thể bỏ nếu không cần) -->
</shape>
